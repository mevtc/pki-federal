{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pki-federal","text":"<p>DoD CAC, Federal PIV, and ECA provider pack for pki-core.</p>"},{"location":"#overview","title":"Overview","text":"<p>pki-federal provides the federal-specific layer for PKI certificate validation and identity extraction. It implements DoD and Federal PKI policy for CAC, PIV, and ECA credentials on top of the generic utilities in pki-core.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Providers -- <code>CAC_PROVIDER</code>, <code>PIV_PROVIDER</code>, <code>ECA_PROVIDER</code> with OID matching, CN parsers, and trust store sources</li> <li>OID registries -- DoD authentication, FPKI PIV authentication, and ECA policy OIDs</li> <li>CN parsers -- CAC dot-format, PIV flexible, and ECA human-readable name parsing</li> <li>Trust store fetchers -- download and parse CA bundles from DISA and repo.fpki.gov</li> <li>Algorithm policy -- <code>SP800_78_ALGORITHM_POLICY</code> with NIST SP 800-78-5 approved algorithms</li> <li>Federal CRLConfig -- <code>CRLConfig</code> subclass with federal default cache directory</li> <li>Federal parse_identity -- defaults to the CAC + PIV registry</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install pki-federal\n</code></pre> <p>This installs <code>pki-core</code> as a dependency. Generic PKI utilities (certificate loading, chain validation, revocation checking, algorithm enforcement) are provided by pki-core.</p>"},{"location":"#relationship-to-pki-core","title":"Relationship to pki-core","text":"<p>pki-federal is a provider pack that plugs into pki-core's provider framework. pki-core provides the generic PKI engine: certificate loading, chain validation, revocation checking, and algorithm enforcement. pki-federal adds the federal-specific policy: DoD/FPKI OID registries, CN parsers for CAC/PIV/ECA name formats, trust store fetchers for DISA and FPKI CA bundles, and the SP 800-78-5 algorithm policy.</p> <pre><code>pki-core          (generic PKI engine)\n  |\n  +-- pki-federal (DoD/FPKI/ECA providers, OIDs, trust stores)\n        |\n        +-- smartcard-auth (application-layer: mTLS, LDAP mapping)\n</code></pre>"},{"location":"#license","title":"License","text":"<p>BSD-3-Clause -- see LICENSE.</p>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to this project will be documented in this file.</p> <p>The format is based on Keep a Changelog, and this project adheres to Semantic Versioning.</p>"},{"location":"changelog/#041-2026-04-03","title":"[0.4.1] - 2026-04-03","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li><code>TrustLevel</code> and <code>CredentialType</code> StrEnums for DoDI 8520.02 trust levels   and federal PKI credential types.</li> <li>Automated PyPI publishing via trusted publisher (OIDC) in release workflow.</li> </ul>"},{"location":"changelog/#040-2026-03-21","title":"[0.4.0] - 2026-03-21","text":""},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li><code>SP800_78_ALGORITHM_POLICY</code> \u2014 <code>AlgorithmPolicy</code> constant with NIST SP 800-78-5   approved algorithms (RSA 2048+, P-256/P-384, SHA-256+).</li> <li><code>SECURITY.md</code> with static analysis suppressions, fuzz testing documentation,   and link to centralized incident response at oss.mevtc.com.</li> <li>Hypothesis fuzz tests for CN parsers, federal providers, and identity extraction.</li> <li>CycloneDX SBOM generation in CI.</li> </ul>"},{"location":"changelog/#changed","title":"Changed","text":"<ul> <li><code>CRLConfig</code> now provides meaningful federal defaults: <code>strict=True</code>,   <code>max_crl_bytes=20 MB</code> (DoD CRLs are large), <code>max_acceptable_age=64800</code>   (18 hours per FIPS 201-3 \u00a72.9.1 revocation timeliness requirement).</li> <li>Depends on <code>pki-core&gt;=0.3.0</code> for <code>RevocationPolicy</code>, <code>AlgorithmPolicy</code>,   and chain validation.</li> </ul>"},{"location":"changelog/#030-2026-03-17","title":"[0.3.0] - 2026-03-17","text":""},{"location":"changelog/#changed_1","title":"Changed","text":"<ul> <li>Renamed project from <code>federal-pki</code> to <code>pki-federal</code></li> <li>Restructured to <code>pki.federal</code> namespace package (<code>src/pki/federal/</code> layout)</li> <li>All imports changed from <code>federal_pki.*</code> to <code>pki.federal.*</code></li> <li>Internal imports updated from <code>pki_core.*</code> to <code>pki.core.*</code></li> <li>Depends on <code>pki-core&gt;=0.1.0</code> (shared <code>pki</code> namespace)</li> </ul>"},{"location":"changelog/#added_2","title":"Added","text":"<ul> <li>ECA (External Certificate Authority) provider definitions</li> <li>Callable-based CN parsing and primary ID selection on provider instances</li> <li>Pre-release checklist in publishing workflow</li> </ul>"},{"location":"changelog/#020-2026-03-16","title":"[0.2.0] - 2026-03-16","text":""},{"location":"changelog/#changed_2","title":"Changed","text":"<ul> <li>Thinned to federal-specific layer on top of pki-core</li> <li>Refactored providers to use callable cn_parser and primary_id_selector</li> <li>Re-exports pki-core API for backward compatibility</li> </ul>"},{"location":"changelog/#010-2025-01-01","title":"[0.1.0] - 2025-01-01","text":""},{"location":"changelog/#added_3","title":"Added","text":"<ul> <li>X.509 certificate parsing with PEM/DER auto-detection</li> <li>CAC identity extraction (EDIPI from CN dot-format)</li> <li>PIV identity extraction (UUID from SAN, FASC-N, multiple CN formats)</li> <li>CRL revocation checking with stale-while-revalidate file-backed cache</li> <li>DoD and Federal PKI CA trust store download, merge, and deduplication</li> <li>Certificate policy OID registries for DoD and FPKI authentication and email signing</li> </ul>"},{"location":"quickstart/","title":"Getting Started","text":""},{"location":"quickstart/#parse-identity-from-a-federal-pki-certificate","title":"Parse identity from a Federal PKI certificate","text":"<p>The simplest use case: extract identity information from a CAC or PIV certificate.</p> <pre><code>from pki.core.certificate import load_certificate\nfrom pki.federal.identity import parse_identity\n\ncert = load_certificate(pem_bytes)\nidentity = parse_identity(cert)\n\nprint(identity.credential_type)  # \"CAC\" or \"PIV\"\nprint(identity.primary_id)       # \"edipi:1234567890\" or \"uuid:...\"\nprint(identity.firstname, identity.lastname)\n</code></pre> <p><code>parse_identity</code> uses the default CAC + PIV provider registry. It matches the certificate's policy OIDs against the registered providers, then extracts identity fields using the matching provider's CN parser.</p>"},{"location":"quickstart/#using-providers-directly","title":"Using providers directly","text":"<p>Providers define the OID matching, CN parsing, and trust store sources for a credential type. You can use the built-in providers or register custom ones.</p> <pre><code>from pki.core.providers import ProviderRegistry\nfrom pki.federal import CAC_PROVIDER, PIV_PROVIDER, ECA_PROVIDER\n\n# Default registry (CAC + PIV)\nfrom pki.federal import default_registry\nregistry = default_registry()\n\n# Full registry (CAC + PIV + ECA)\nfrom pki.federal import full_registry\nregistry = full_registry()\n\n# Custom registry\nregistry = ProviderRegistry()\nregistry.register(CAC_PROVIDER)\nregistry.register(PIV_PROVIDER)\nregistry.register(ECA_PROVIDER)\n</code></pre>"},{"location":"quickstart/#building-a-trust-store","title":"Building a trust store","text":"<p>Download and merge CA bundles from DISA and the Federal PKI into a single PEM file for chain validation.</p> <pre><code>from pki.federal.trust_store import build_ca_bundle\n\npem_bundle, stats = build_ca_bundle(output_path=\"/etc/ssl/dod-fpki-bundle.pem\")\nprint(f\"Loaded {stats['total']} certificates, {stats['unique']} unique\")\n</code></pre>"},{"location":"quickstart/#algorithm-policy","title":"Algorithm policy","text":"<p><code>SP800_78_ALGORITHM_POLICY</code> enforces NIST SP 800-78-5 approved algorithms: RSA 2048+, ECDSA P-256/P-384, and SHA-256 or stronger hashes.</p> <pre><code>from pki.federal import SP800_78_ALGORITHM_POLICY\n\n# Use in a CertificatePolicy for validation\nfrom pki.core.validation import CertificatePolicy\n\npolicy = CertificatePolicy(\n    algorithm_policy=SP800_78_ALGORITHM_POLICY,\n)\n</code></pre> <p>For stricter deployments, create a custom <code>AlgorithmPolicy</code>:</p> <pre><code>from pki.core.algorithms import AlgorithmPolicy\n\necc_only = AlgorithmPolicy(\n    min_rsa_bits=0,\n    allowed_curves=frozenset({\"secp384r1\"}),\n    allowed_hashes=frozenset({\"sha384\", \"sha512\"}),\n)\n</code></pre>"},{"location":"quickstart/#full-fips-201-3-validation-pipeline","title":"Full FIPS 201-3 validation pipeline","text":"<p>A production configuration implementing PKI-AUTH (FIPS 201-3) with chain validation, SP 800-78 algorithm enforcement, and CRL + OCSP revocation checking.</p> <pre><code>from pki.core.certificate import load_certificate\nfrom pki.core.crl import load_ca_certs_from_pem\nfrom pki.core.revocation import CRL, OCSP, RevocationPolicy\nfrom pki.core.validation import CertificatePolicy, ValidationStatus, validate_certificate\nfrom pki.federal import SP800_78_ALGORITHM_POLICY, default_registry\nfrom pki.federal.crl import CRLConfig\nfrom pki.federal.trust_store import build_ca_bundle\n\n# Build or load the Federal PKI CA bundle\npem_bundle, stats = build_ca_bundle(output_path=\"/etc/pki/dod-fpki-bundle.pem\")\nca_certs = load_ca_certs_from_pem(pem_bundle)\n\n# Load the client certificate (e.g., from mTLS header)\ncert = load_certificate(pem_bytes)\n\n# Configure the full validation pipeline\npolicy = CertificatePolicy(\n    check_chain=True,\n    trust_store=ca_certs,\n    algorithm_policy=SP800_78_ALGORITHM_POLICY,\n    registry=default_registry(),\n    revocation=RevocationPolicy(\n        checks=(CRL, OCSP),\n        issuer_certs=ca_certs,\n        crl_config=CRLConfig(cache_dir=\"/var/cache/pki/crls\"),\n    ),\n)\n\nresult = validate_certificate(cert, policy)\n\nif result.status == ValidationStatus.VALID:\n    identity = result.identity\n    print(f\"Authenticated: {identity.credential_type} -- {identity.primary_id}\")\n    print(f\"Name: {identity.firstname} {identity.lastname}\")\nelse:\n    print(f\"Rejected: {result.status} -- {result.error}\")\n</code></pre>"},{"location":"quickstart/#crl-configuration","title":"CRL configuration","text":"<p>The federal <code>CRLConfig</code> provides sensible defaults for DoD/FPKI environments: strict mode, 20 MB max CRL size (DoD CRLs are large), and 18-hour max age per FIPS 201-3.</p> <pre><code>from pki.federal.crl import CRLConfig\n\nconfig = CRLConfig(cache_dir=\"/var/cache/pki/crls\")\n</code></pre>"},{"location":"api/","title":"API Reference","text":"<p>Auto-generated documentation from source code docstrings.</p>"},{"location":"api/#modules","title":"Modules","text":"Module Description Providers CAC, PIV, and ECA provider definitions Identity Federal identity extraction from certificates CN Parsers Common Name parsing for CAC, PIV, and ECA formats OIDs Certificate policy OID registries CRL Federal CRL configuration defaults Algorithms SP 800-78-5 algorithm policy Trust Store CA bundle download and management Trust Trust levels and credential types"},{"location":"api/algorithms/","title":"Algorithms","text":"<p>NIST SP 800-78-5 algorithm policy for Federal PKI certificate validation.</p>"},{"location":"api/algorithms/#pki.federal.algorithms","title":"<code>algorithms</code>","text":"<p>SP 800-78 algorithm policy for Federal PKI certificates.</p> <p>Defines approved cryptographic algorithms per NIST SP 800-78 (Cryptographic Algorithms and Key Sizes for Personal Identity Verification).  The defaults match SP 800-78-5 requirements for PIV authentication certificates.</p> <p>Applications may override with stricter requirements::</p> <pre><code>from pki.core.algorithms import AlgorithmPolicy\n\n# ECC P-384 only, no RSA\necc_only = AlgorithmPolicy(\n    min_rsa_bits=0,\n    allowed_curves=frozenset({\"secp384r1\"}),\n)\n</code></pre>"},{"location":"api/cn_parsers/","title":"CN Parsers","text":"<p>Common Name parsing functions for CAC dot-format, PIV flexible format, and ECA human-readable name format.</p>"},{"location":"api/cn_parsers/#pki.federal.cn_parsers","title":"<code>cn_parsers</code>","text":"<p>CN parsing strategies for different credential types.</p> <p>Each strategy extracts first/last name and identifiers from the certificate Subject CN field according to the conventions of that credential ecosystem.</p>"},{"location":"api/crl/","title":"CRL","text":"<p>Federal CRL configuration with defaults for DoD/FPKI environments.</p>"},{"location":"api/crl/#pki.federal.crl","title":"<code>crl</code>","text":"<p>Federal CRL configuration with FIPS 201-3 compliant defaults.</p> <p>Provides sensible defaults for DoD and Federal PKI environments:</p> <ul> <li><code>strict=True</code> \u2014 always verify CRL signatures.</li> <li><code>max_crl_bytes=20 MB</code> \u2014 DoD CRLs can be significantly larger than   typical enterprise CRLs.</li> <li><code>max_acceptable_age=64800</code> (18 hours) \u2014 FIPS 201-3 Section 2.9.1   requires that revocation procedures for lost, stolen, or compromised   cards be completed within 18 hours of notification.  A CRL older than   this is too stale to trust even as a fallback.</li> </ul>"},{"location":"api/crl/#pki.federal.crl.CRLConfig","title":"<code>CRLConfig(app_name='pki-federal', cache_dir='', cache_ttl=3600, strict=True, fetch_timeout=10, max_crl_bytes=20 * 1024 * 1024, max_acceptable_age=64800)</code>  <code>dataclass</code>","text":"<p>               Bases: <code>CRLConfig</code></p> <p>CRLConfig with Federal PKI defaults.</p> Overrides <p>app_name: <code>pki-federal</code> (cache at <code>~/.cache/pki-federal/crls</code>). strict: <code>True</code> \u2014 CRL signature verification required. max_crl_bytes: 20 MB \u2014 accommodates large DoD CRLs. max_acceptable_age: 64800 seconds (18 hours) \u2014 per FIPS 201-3     Section 2.9.1 revocation timeliness requirement.</p>"},{"location":"api/identity/","title":"Identity","text":"<p>Federal identity extraction from X.509 certificates using the provider registry.</p>"},{"location":"api/identity/#pki.federal.identity","title":"<code>identity</code>","text":"<p>Federal identity extraction with default registry.</p>"},{"location":"api/identity/#pki.federal.identity.parse_identity","title":"<code>parse_identity(cert, registry=None)</code>","text":"<p>Parse an x509 certificate into a CertIdentity.</p> <p>Defaults to the federal default_registry (CAC + PIV) if no registry is provided.</p> Source code in <code>src/pki/federal/identity.py</code> <pre><code>def parse_identity(\n    cert: x509.Certificate,\n    registry: ProviderRegistry | None = None,\n) -&gt; CertIdentity:\n    \"\"\"Parse an x509 certificate into a CertIdentity.\n\n    Defaults to the federal default_registry (CAC + PIV) if no registry\n    is provided.\n    \"\"\"\n    if registry is None:\n        from .providers import default_registry\n\n        registry = default_registry()\n    return _core_parse_identity(cert, registry=registry)\n</code></pre>"},{"location":"api/oids/","title":"OIDs","text":"<p>Certificate policy OID registries for DoD authentication, FPKI PIV authentication, and ECA policies.</p>"},{"location":"api/oids/#pki.federal.oids","title":"<code>oids</code>","text":"<p>DoD and Federal PKI certificate policy OID registries.</p> <p>OID definitions are sourced from:</p> <ul> <li> <p>DoD PKI OIDs: DISA PKI/PKE Reference Materials   https://public.cyber.mil/pki-pke/interoperability/</p> </li> <li> <p>Federal PKI OIDs: X.509 Certificate Policy for the U.S. FPKI Common Policy Framework   https://www.idmanagement.gov/topics/fpki/</p> </li> <li> <p>ECA OIDs: DoD ECA Certificate Policy   https://public.cyber.mil/eca/</p> </li> <li> <p>OID assignments under arc 2.16.840.1.101.2 (DoD) and 2.16.840.1.101.3 (FPKI)   are registered in the FPKI OID registry maintained by GSA/FICAM:   https://www.idmanagement.gov/fpki/</p> </li> </ul>"},{"location":"api/providers/","title":"Providers","text":"<p>CAC, PIV, and ECA provider definitions with OID matching, CN parsers, and trust store sources.</p>"},{"location":"api/providers/#pki.federal.providers","title":"<code>providers</code>","text":"<p>Federal PKI authentication provider instances and registries.</p>"},{"location":"api/providers/#pki.federal.providers.CNParseStrategy","title":"<code>CNParseStrategy</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Deprecated: AuthProvider now uses cn_parser callable.</p>"},{"location":"api/providers/#pki.federal.providers.PrimaryIDStrategy","title":"<code>PrimaryIDStrategy</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Deprecated: AuthProvider now uses primary_id_selector callable.</p>"},{"location":"api/providers/#pki.federal.providers.default_registry","title":"<code>default_registry()</code>","text":"<p>Create a registry with CAC + PIV enabled (backward-compatible default).</p> Source code in <code>src/pki/federal/providers.py</code> <pre><code>def default_registry() -&gt; ProviderRegistry:\n    \"\"\"Create a registry with CAC + PIV enabled (backward-compatible default).\"\"\"\n    reg = ProviderRegistry()\n    reg.register(CAC_PROVIDER)\n    reg.register(PIV_PROVIDER)\n    return reg\n</code></pre>"},{"location":"api/providers/#pki.federal.providers.full_registry","title":"<code>full_registry()</code>","text":"<p>Create a registry with all built-in providers.</p> Source code in <code>src/pki/federal/providers.py</code> <pre><code>def full_registry() -&gt; ProviderRegistry:\n    \"\"\"Create a registry with all built-in providers.\"\"\"\n    reg = ProviderRegistry()\n    for provider in BUILTIN_PROVIDERS.values():\n        reg.register(provider)\n    return reg\n</code></pre>"},{"location":"api/trust/","title":"Trust","text":"<p>Trust levels and credential type enumerations for DoDI 8520.02 compliance.</p>"},{"location":"api/trust/#pki.federal.trust","title":"<code>trust</code>","text":"<p>DoDI 8520.02 trust levels and credential types.</p> <p>Trust levels map authentication methods to assurance levels per DoDI 8520.02 (Public Key Infrastructure and Public Key Enabling). PKI authentication is mandatory for DoD systems; password fallback is only permitted when PKI is \"not technically feasible\" and must be documented in the system authorization package (ATO).</p>"},{"location":"api/trust/#pki.federal.trust.TrustLevel","title":"<code>TrustLevel</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Authentication assurance level per DoDI 8520.02.</p> <p>Ordered from highest to lowest assurance.  Supports comparison::</p> <pre><code>if user.trust_level &lt; TrustLevel.HIGH:\n    raise HTTPException(403, \"Requires smartcard\")\n</code></pre>"},{"location":"api/trust/#pki.federal.trust.TrustLevel.__lt__","title":"<code>__lt__(other)</code>","text":"<p>Compare trust levels by assurance ordering.</p> Source code in <code>src/pki/federal/trust.py</code> <pre><code>def __lt__(self, other):\n    \"\"\"Compare trust levels by assurance ordering.\"\"\"\n    if not isinstance(other, TrustLevel):\n        return NotImplemented\n    order = [TrustLevel.NONE, TrustLevel.BASIC, TrustLevel.MEDIUM, TrustLevel.HIGH]\n    return order.index(self) &lt; order.index(other)\n</code></pre>"},{"location":"api/trust/#pki.federal.trust.TrustLevel.__le__","title":"<code>__le__(other)</code>","text":"<p>Return True if this trust level is equal or lower assurance.</p> Source code in <code>src/pki/federal/trust.py</code> <pre><code>def __le__(self, other):\n    \"\"\"Return True if this trust level is equal or lower assurance.\"\"\"\n    return self == other or self &lt; other\n</code></pre>"},{"location":"api/trust/#pki.federal.trust.TrustLevel.__gt__","title":"<code>__gt__(other)</code>","text":"<p>Return True if this trust level is higher assurance.</p> Source code in <code>src/pki/federal/trust.py</code> <pre><code>def __gt__(self, other):\n    \"\"\"Return True if this trust level is higher assurance.\"\"\"\n    if not isinstance(other, TrustLevel):\n        return NotImplemented\n    return not self &lt;= other\n</code></pre>"},{"location":"api/trust/#pki.federal.trust.TrustLevel.__ge__","title":"<code>__ge__(other)</code>","text":"<p>Return True if this trust level is equal or higher assurance.</p> Source code in <code>src/pki/federal/trust.py</code> <pre><code>def __ge__(self, other):\n    \"\"\"Return True if this trust level is equal or higher assurance.\"\"\"\n    return self == other or self &gt; other\n</code></pre>"},{"location":"api/trust/#pki.federal.trust.CredentialType","title":"<code>CredentialType</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Federal PKI credential types.</p> <p>Corresponds to the <code>name</code> field on <code>AuthProvider</code> instances in pki-federal's provider registry.</p>"},{"location":"api/trust_store/","title":"Trust Store","text":"<p>CA bundle download, parsing, and management for DISA and Federal PKI trust stores.</p>"},{"location":"api/trust_store/#pki.federal.trust_store","title":"<code>trust_store</code>","text":"<p>DoD and Federal PKI CA trust store management.</p> <p>Downloads, parses, deduplicates, and merges CA certificate bundles from DISA (DoD), repo.fpki.gov (Federal PKI), and other provider sources.</p> <p>Controls: SC-12 (Cryptographic Key Management), IA-5(2) (PKI-Based Auth)</p>"},{"location":"api/trust_store/#pki.federal.trust_store.fetch_dod_certs","title":"<code>fetch_dod_certs(url=None)</code>","text":"<p>Download and parse DoD CA certificates from DISA PKCS7 ZIP.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str | None</code> <p>Override the default DoD PKI ZIP URL.</p> <code>None</code> <p>Returns:</p> Type Description <code>list</code> <p>List of cryptography x509.Certificate objects.</p> Source code in <code>src/pki/federal/trust_store.py</code> <pre><code>def fetch_dod_certs(url: str | None = None) -&gt; list:\n    \"\"\"Download and parse DoD CA certificates from DISA PKCS7 ZIP.\n\n    Args:\n        url: Override the default DoD PKI ZIP URL.\n\n    Returns:\n        List of cryptography x509.Certificate objects.\n    \"\"\"\n    certs = []\n    zip_data = download(url or DOD_PKI_ZIP_URL)\n\n    with zipfile.ZipFile(io.BytesIO(zip_data)) as zf:\n        for name in zf.namelist():\n            if not (name.endswith(\".p7b\") or name.endswith(\".p7c\")):\n                continue\n            if \"..\" in name or name.startswith(\"/\"):\n                logger.warning(\"Skipping suspicious ZIP entry: %s\", name)\n                continue\n\n            p7_data = zf.read(name)\n            try:\n                parsed = load_pem_pkcs7_certificates(p7_data)\n                logger.info(\"Parsed %d certs from %s (PEM PKCS7)\", len(parsed), name)\n                certs.extend(parsed)\n            except Exception:\n                try:\n                    parsed = load_der_pkcs7_certificates(p7_data)\n                    logger.info(\"Parsed %d certs from %s (DER PKCS7)\", len(parsed), name)\n                    certs.extend(parsed)\n                except Exception as e:\n                    logger.warning(\"Could not parse %s: %s\", name, e)\n\n    logger.info(\"Total DoD CA certificates: %d\", len(certs))\n    return certs\n</code></pre>"},{"location":"api/trust_store/#pki.federal.trust_store.fetch_fpki_certs","title":"<code>fetch_fpki_certs()</code>","text":"<p>Download and parse Federal PKI CA certificates.</p> <p>Returns:</p> Type Description <code>list</code> <p>List of cryptography x509.Certificate objects.</p> Source code in <code>src/pki/federal/trust_store.py</code> <pre><code>def fetch_fpki_certs() -&gt; list:\n    \"\"\"Download and parse Federal PKI CA certificates.\n\n    Returns:\n        List of cryptography x509.Certificate objects.\n    \"\"\"\n    certs = []\n\n    # Federal Common Policy CA G2 root (DER X.509)\n    der_data = download(FCPCA_G2_URL)\n    cert = load_der_x509_certificate(der_data)\n    certs.append(cert)\n    logger.info(\"Loaded FCPCA G2 root: %s\", cert.subject)\n\n    # Federal Bridge CA G4 bundles (PKCS7/DER)\n    for url in [FBCA_G4_ISSUED_TO_URL, FBCA_G4_ISSUED_BY_URL]:\n        p7c_data = download(url)\n        parsed = load_der_pkcs7_certificates(p7c_data)\n        logger.info(\"Parsed %d certs from %s\", len(parsed), url)\n        certs.extend(parsed)\n\n    logger.info(\"Total FPKI CA certificates: %d\", len(certs))\n    return certs\n</code></pre>"},{"location":"api/trust_store/#pki.federal.trust_store.build_ca_bundle","title":"<code>build_ca_bundle(output_path=None, filter_fn=None)</code>","text":"<p>Fetch DoD + FPKI certs, merge, deduplicate, and optionally write to file.</p> <p>Parameters:</p> Name Type Description Default <code>output_path</code> <code>str | None</code> <p>If provided, write the PEM bundle to this path.</p> <code>None</code> <code>filter_fn</code> <p>Optional callable(cert) -&gt; bool for filtering.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[str, dict]</code> <p>Tuple of (pem_bundle_string, stats_dict).</p> Source code in <code>src/pki/federal/trust_store.py</code> <pre><code>def build_ca_bundle(\n    output_path: str | None = None,\n    filter_fn=None,\n) -&gt; tuple[str, dict]:\n    \"\"\"Fetch DoD + FPKI certs, merge, deduplicate, and optionally write to file.\n\n    Args:\n        output_path: If provided, write the PEM bundle to this path.\n        filter_fn: Optional callable(cert) -&gt; bool for filtering.\n\n    Returns:\n        Tuple of (pem_bundle_string, stats_dict).\n    \"\"\"\n    dod_certs = fetch_dod_certs()\n    fpki_certs = fetch_fpki_certs()\n\n    if not dod_certs and not fpki_certs:\n        raise RuntimeError(\"No certificates fetched from either source\")\n\n    pem_bundle, stats = merge_and_deduplicate(\n        [(\"DoD\", dod_certs), (\"FPKI\", fpki_certs)],\n        filter_fn=filter_fn,\n    )\n\n    if output_path:\n        from pathlib import Path\n\n        path = Path(output_path)\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(pem_bundle)\n        logger.info(\"CA bundle written to %s\", output_path)\n\n    return pem_bundle, stats\n</code></pre>"}]}